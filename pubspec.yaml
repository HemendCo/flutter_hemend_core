name: hemend
description: base package of Hemend projects
version: 0.0.1
homepage: https://hemend.com/

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=2.10.1"

dependencies:
  flutter:
    sdk: flutter
  device_info_plus:
  http:
  package_info_plus:
  build:
  meta: ^1.7.0
  source_gen:

  dio: ^4.0.1
  web_socket_channel: ^2.1.0
  path_provider: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
